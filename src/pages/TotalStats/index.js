import React from 'react';
import TotalOrders from './../../components/TotalOrders';

const TotalStats = ({}) => {
    return (
        <div>
        <TotalOrders />
        </div>
    )
}
export default TotalStats